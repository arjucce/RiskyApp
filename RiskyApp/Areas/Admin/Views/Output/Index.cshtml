@model RiskyApp.Models.VM.OutputVM

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}


<form asp-action="Search" method="post">
    <div class="card">
    <div class="card-header">Available Data</div>
    <div class="card-body">
        <div class="form-group">
            <label class="control-label text-info">Process Date</label>
            <input type="date" name="dt" id="dt" class="form-control" />
        </div>
        <div class="form-group">
            <label class="control-label text-info">Select Department</label>
            @Html.DropDownListFor(m => m.DepartmentId, Model.DepartmentList, "Please Select Department", new { @class = "form-control" })
        </div>
        <div class="form-group">
            <label class="control-label text-info">Select Operation Category</label>
            @Html.DropDownListFor(m => m.CategoryId, Model.CategoryList, "Please Select Operation Category", new { @class = "form-control" })
        </div>
        <br />
         <div class="form-group">
            <input type="submit" value="Search" class="btn btn-primary form-control" />
        </div>
        <br />
        <br />
        <div class="form-group row">
            <table id="input-table" class="table">
                <thead>
                    <tr>
                        <th>Reporting Category</th>
                        <th>Operation Name</th>
                        <th>Amount</th>
                        <th>Uploaded File</th>
                        <th></th>
                    </tr>
                </thead>
                <tbody>
                        @*@foreach(var item in Model.Datas)
                        {
                            <tr>
                                <td><input type="text" readonly class="form-control-plaintext" value="@item.OperationTitle"></td>
                                <td><input type="text" readonly class="form-control-plaintext" value="@item.Amount"></td>
                                <td><input type="text" readonly class="form-control-plaintext" value="@item.Url"></td>
                                <td> @Html.ActionLink("Download", "Download", new { Format = item.Url }) </td>
                            </tr>
                        }*@
                        @*@for (int j = 0; j < Model.Count; j++)
                    {
                        @if (Model[j].OperationType == "Upload")
                        {
                            <tr>
                                <td>@Html.TextBox("dps[" + @j + "].OperationName", Model[j].OperationName,  new { @class = "form-control", @readonly = "readonly" })</td>
                                <td><input type="file" class="form-control" name="files" multiple /></td>
                                <td>@Html.Hidden("Download", "Download", new { Format = @Model[j].FormatUrl, @class = "btn btn-primary" }) </td>
                                <td>@Html.Hidden("dps[" + @j + "].DepartmentID", Model[j].DepartmentID,  new { @class = "form-control hidden" })</td>
                                <td>@Html.Hidden("dps[" + @j + "].OperationType", Model[j].OperationType,  new { @class = "form-control hidden" })</td>
                                <td>@Html.Hidden("dps[" + @j + "].CategoryId", Model[j].CategoryId,  new { @class = "form-control hidden" })</td>
                            </tr>
                        }                        
                    }*@
                </tbody>
            </table>
        </div>
    </div>
    </div>    
</form>
